version: 2.1
jobs:
  build:
    docker:
      - image: cimg/openjdk:25.0
    steps:
      - checkout
      - run:
          name: build
          command: ./gradlew build -x test
      - run:
          name: test
          command: ./gradlew test
workflows:
  build-and-test:
    jobs:
      - build